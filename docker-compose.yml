version: '3.8'

services:
  scorig-app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: credit-scoring-analyzer
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - TZ=UTC
