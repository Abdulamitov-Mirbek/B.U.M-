generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:mirbek777389866@localhost:5432/credit_scoring?schema=public"
}

model Application {
  id                 String   @id @default(cuid())
  userId             String
  age                Int
  monthlyIncome      Float
  loanAmount         Float
  loanTermMonths     Int
  creditHistoryYears Float
  currentDebt        Float
  employmentYears    Float
  dependents         Int
  result             String
  probability        Float
  score              Float
  riskLevel          String
  recommendedAmount  Float
  createdAt          DateTime @default(now())
}
